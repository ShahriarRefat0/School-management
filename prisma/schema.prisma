
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}


model School {
  id               String   @id @default(cuid())
  schoolName       String
  slug             String   @unique
  schoolEmail      String   @unique
  phone            String?
  address          String?
  plan             String   @default("basic")
  duration         String   @default("12")
  
  // New Fields
  schoolCategory   String
  expectedStudents Int?    
  registrationId   String   @unique
  facebookUrl      String?
  websiteUrl       String?
  language         String   @default("english")
  
  // Admin Info
  adminName        String
  adminEmail       String   @unique
  adminPassword    String  
  
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}

// prisma/schema.prisma

model SupportTicket {
  id            String   @id @default(cuid())
  subject       String
  description   String
  priority      String   @default("low") 
  status        String   @default("open") // open, closed, pending
  schoolId      String?  // কোন স্কুল থেকে টিকিট আসছে
  userEmail     String   // কে টিকিট ক্রিয়েট করেছে
  attachmentUrl String?  // ইমেজ বা স্ক্রিনশট স্টোর করার জন্য এই ফিল্ডটি যোগ করা হলো
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}